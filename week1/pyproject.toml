[project]
name = "week1"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "bs4>=0.0.2",
    "pydantic>=2.13.4",
    "ruff>=0.15.12",
]

[tool.uv]
required-version = ">=0.8.0,<0.9.0"
